# Drive Vision

Semantic segmentation for autonomous driving perception — a from-scratch implementation and comparative study of U-Net architectures on the KITTI dataset.

## Overview

This project explores how architectural choices affect semantic segmentation performance on urban driving scenes. Four models are implemented and benchmarked:

- U-Net (from scratch) — baseline
- Attention U-Net — architectural modification with learned skip-connection gating
- ResNet-encoder U-Net — transfer learning from ImageNet-pretrained backbone
- DeepLabV3+ (fine-tuned) — state-of-the-art reference

## Status

Work in progress. Currently: pro# Drive Vision

Semantic segmentation for autonomous driving perception — a from-scratch implementation and comparative study of U-Net architectures on the KITTI dataset.

## Overview

This project explores how architectural choices affect semantic segmentation performance on urban driving scenes. Four models are implemented and benchmarked:

- U-Net (from scratch) — baseline
- Attention U-Net — architectural modification with learned skip-connection gating
- ResNet-encoder U-Net — transfer learning from ImageNet-pretrained backbone
- DeepLabV3+ (fine-tuned) — state-of-the-art reference

## Status

Work in progress. Currently: project setup complete.

## Stack

TensorFlow 2 / Keras, Python 3, KITTI Semantic Segmentation dataset.

## Results

_To be added as experiments complete._ject setup complete.

## Stack

TensorFlow 2 / Keras, Python 3, KITTI Semantic Segmentation dataset.

## Results

_To be added as experiments complete._